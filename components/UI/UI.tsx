import Image from 'next/image';import Link from 'next/link';import s from './UI.module.css';
export function Button({href,children,light=false}:{href:string;children:React.ReactNode;light?:boolean}){return <Link className={`${s.button} ${light?s.light:''}`} href={href}>{children}<b>→</b></Link>}
export function Hero({title,subtitle,image,compact=false}:{title:string;subtitle?:string;image:string;compact?:boolean}){return <section className={`${s.hero} ${compact?s.compact:''}`} style={{backgroundImage:`linear-gradient(#052f3e70,#052f3e70),url("${image}")`}}><div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}<Button href="/contact-us-vm" light>Contact Now</Button></div></section>}
export function Split({title,children,image,reverse=false,tone='cream',eyebrow}:{title:string;children:React.ReactNode;image:string;reverse?:boolean;tone?:string;eyebrow?:string}){return <section className={`${s.split} ${reverse?s.reverse:''} ${s[tone]||''}`}><div className={s.photo}><Image src={image} alt="" fill sizes="(max-width: 800px) 100vw, 50vw"/></div><div className={s.copy}>{eyebrow&&<span>{eyebrow}</span>}<h2>{title}</h2><div className={s.rule}/>{children}</div></section>}
export function PageIntro({eyebrow,title,children}:{eyebrow?:string;title:string;children:React.ReactNode}){return <section className={s.intro}>{eyebrow&&<span>{eyebrow}</span>}<h1>{title}</h1><div>{children}</div></section>}
export function Cards({items}:{items:{title:string;text:string}[]}){return <section className={s.cards}>{items.map((x,i)=><article key={x.title}><b>{String(i+1).padStart(2,'0')}</b><h3>{x.title}</h3><p>{x.text}</p></article>)}</section>}
